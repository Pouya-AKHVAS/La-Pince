// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

//Aucune output personnalisée.
//✔ Résultat
//Prisma Client est toujours installé dans le répertoire standard : node_modules/@prisma/client

generator client {
  provider = "prisma-client-js"
  // output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id @default(autoincrement())
  first_name String
  last_name String
  email     String   @unique
  password  String
  photo     String?
  categories Category[]
  tokens     RefreshToken[]
  createdAt DateTime @default(now())
<<<<<<< HEAD
  updatedAt  DateTime @updatedAt
}

model Category {
  id          Int      @id @default(autoincrement())
  name        String   
  color       String?
  icon        String?
  type        String  ' expense' | 'income' // type de catégorie : dépense ou revenu
  is_default  Boolean  @default(false)
  id_user     Int?
  user        User?    @relation(fields: [id_user], references: [id])
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

//unique ici pour que la catégorie puisse être utilisé par plusieurs utilisateurs  
  @@unique ([name, id_user])
  @@map("category")
}


model RefreshToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  createdAt DateTime @default(now())
}


=======
  updatedAt  DateTime @updatedAt()
  refreshTokens RefreshToken[]


  @@map("users")
}

model RefreshToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt()
  expiresAt DateTime

  @@map("refresh_tokens")
}
>>>>>>> dev
